services:
  app:
    build: .
    env_file:
      - .env
    # exposto só no loopback: o Caddy (fora do compose) faz TLS e proxy reverso
    ports:
      - "127.0.0.1:5000:5000"
    restart: unless-stopped
